// Transforma esta declaración de función a una función de expresión, en un codigo funcional.
/*
function example(a, b, c){
return a+b+c
}
 */
const suma = function (a, b, c) {
  return a + b + c;
  
};

suma(1, 2, 3)